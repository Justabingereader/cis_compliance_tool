"""History panel — deferred until core pipeline is complete."""

import streamlit as st


def render() -> None:
    st.header("History")
    st.info("History view will be implemented after the core engine.")